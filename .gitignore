# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
# NOTE: notebooks/*.ipynb IS committed for submission (with output cells preserved
# for grading). The Jupytext `.py` files are the source-of-truth template;
# `.ipynb` is the student's executed deliverable.

# Lab artifacts. Commit the small files required by the submission verifier,
# but continue ignoring checkpoints/weights and other regenerated outputs.
data/pref/*
!data/pref/train.parquet
!data/pref/eval.parquet
data/eval_prompts.json
adapters/sft-mini/*
!adapters/sft-mini/adapter_config.json
adapters/dpo/*
!adapters/dpo/adapter_config.json
!adapters/dpo/dpo_metrics.json
adapters/orpo/
gguf/
*.gguf
runs/
wandb/

# HuggingFace cache (Colab + local — model weights are huge)
.cache/
hf_cache/

# Raw Kaggle artifact bundle unpacked locally. It contains duplicated adapters,
# generated trainer caches, and >100 MB weight files; keep it as a local backup
# but never stage it for GitHub.
lab22/

# Env / secrets
.env
.env.local
*.token

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
